import Link from "next/link";
import { notFound, redirect } from "next/navigation";
import { getSaleAction } from "@/app/actions/f2";
import { getCurrentOwner } from "@/lib/supabase/owner";
import { InternalShell } from "@/components/internal-shell";
import { ErrorState, PageHeader, PaymentBadge, rupiah } from "@/components/ui";
export default async function SaleDetailPage({ params, searchParams }: { params: Promise<{ id: string }>; searchParams: Promise<{ created?: string }> }) { const owner = await getCurrentOwner(); if (!owner) redirect("/login?next=/penjualan"); const { id } = await params; const result = await getSaleAction({ id }); if (!result.ok && result.error.code === "not_found") notFound(); const created = (await searchParams).created === "1"; return <InternalShell ownerName={owner.display_name}><main className="page">{!result.ok ? <ErrorState {...result.error} retryHref={`/penjualan/${id}`}/> : <>{created && <p className="notice success" role="status">Penjualan berhasil disimpan.</p>}<PageHeader title={result.data.invoiceNumber} description={`${result.data.customerName} · ${result.data.transactionDate}`} action={<PaymentBadge status={result.data.paymentStatus}/>}/><section className="invoice-grid"><div className="invoice-main"><h2>Item</h2>{result.data.items.map((item) => <div className="invoice-item" key={item.id}><div><strong>{item.description}</strong><span>{item.pricingBasis === "crate" ? `${item.crateQuantity} peti` : `${item.weightKg} kg`} × {rupiah(item.unitPriceRupiah)}</span></div><strong>{rupiah(item.subtotalRupiah)}</strong></div>)}{result.data.notes && <div className="invoice-notes"><h3>Catatan</h3><p>{result.data.notes}</p></div>}</div><aside className="summary"><h2>Ringkasan invoice</h2><Line label="Subtotal" value={result.data.subtotalRupiah}/><Line label="Diskon" value={-result.data.discountRupiah}/><Line label="Biaya" value={result.data.feeRupiah}/><Line label="Total" value={result.data.totalRupiah} strong/><Line label="Dibayar" value={result.data.paidRupiah}/><Line label="Sisa" value={result.data.remainingRupiah} strong/>{result.data.dueDate && <p>Jatuh tempo: <strong>{result.data.dueDate}</strong></p>}</aside></section><section className="payments"><h2>Pembayaran awal</h2>{result.data.payments.length ? result.data.payments.map((payment) => <div className="data-row" key={payment.id}><span>{payment.method === "cash" ? "Tunai" : payment.method === "transfer" ? "Transfer" : "Lainnya"}</span><strong>{rupiah(payment.amountRupiah)}</strong></div>) : <p>Belum ada pembayaran.</p>}</section><Link className="text-link" href="/penjualan">Kembali ke daftar penjualan</Link></>}</main></InternalShell>; }
function Line({ label, value, strong }: { label: string; value: number; strong?: boolean }) { return <div className={strong ? "money strong" : "money"}><span>{label}</span><span>{rupiah(value)}</span></div>; }
